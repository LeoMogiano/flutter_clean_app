import 'package:flutter/material.dart';

class AppColors {
  // Primaries
  static const primary = Color(0xFF0055FF);
  static const primaryLight = Color(0xFF4D7CFF);
  static const primaryDark = Color(0xFF0033AA);

  // Secondaries
  static const secondary = Color(0xFFFF5500);
  static const secondaryLight = Color(0xFFFF7F3F);
  static const secondaryDark = Color(0xFFAA3300);

  // Neutrals (Light)
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const error = Color(0xFFFF3333);
  static const Color onPrimary = Colors.white;
  static const Color onSecondary = Colors.white;
  static const Color onBackground = Colors.black87;
  static const Color onSurface = Colors.black87;

  // Neutrals (Dark)
  static const backgroundDark = Color(0xFF181A20);
  static const surfaceDark = Color(0xFF23262F);
  static const errorDark = Color(0xFFFF6666);
  static const Color onPrimaryDark = Colors.white;
  static const Color onSecondaryDark = Colors.white;
  static const Color onBackgroundDark = Colors.white70;
  static const Color onSurfaceDark = Colors.white70;
}
